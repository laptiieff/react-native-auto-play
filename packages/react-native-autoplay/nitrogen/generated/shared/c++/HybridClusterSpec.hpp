///
/// HybridClusterSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ClusterEventName` to properly resolve imports.
namespace margelo::nitro::swe::iternio::reactnativeautoplay { enum class ClusterEventName; }
// Forward declaration of `NitroAttributedString` to properly resolve imports.
namespace margelo::nitro::swe::iternio::reactnativeautoplay { struct NitroAttributedString; }
// Forward declaration of `ColorScheme` to properly resolve imports.
namespace margelo::nitro::swe::iternio::reactnativeautoplay { enum class ColorScheme; }
// Forward declaration of `ZoomEvent` to properly resolve imports.
namespace margelo::nitro::swe::iternio::reactnativeautoplay { enum class ZoomEvent; }

#include <functional>
#include "ClusterEventName.hpp"
#include <string>
#include <NitroModules/Promise.hpp>
#include "NitroAttributedString.hpp"
#include <vector>
#include "ColorScheme.hpp"
#include "ZoomEvent.hpp"

namespace margelo::nitro::swe::iternio::reactnativeautoplay {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Cluster`
   * Inherit this class to create instances of `HybridClusterSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridCluster: public HybridClusterSpec {
   * public:
   *   HybridCluster(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridClusterSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridClusterSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridClusterSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::function<void()> addListener(ClusterEventName eventType, const std::function<void(const std::string& /* clusterId */)>& callback) = 0;
      virtual std::shared_ptr<Promise<void>> initRootView(const std::string& clusterId) = 0;
      virtual void setAttributedInactiveDescriptionVariants(const std::string& clusterId, const std::vector<NitroAttributedString>& attributedInactiveDescriptionVariants) = 0;
      virtual std::function<void()> addListenerColorScheme(const std::function<void(const std::string& /* clusterId */, ColorScheme /* payload */)>& callback) = 0;
      virtual std::function<void()> addListenerZoom(const std::function<void(const std::string& /* clusterId */, ZoomEvent /* payload */)>& callback) = 0;
      virtual std::function<void()> addListenerCompass(const std::function<void(const std::string& /* clusterId */, bool /* payload */)>& callback) = 0;
      virtual std::function<void()> addListenerSpeedLimit(const std::function<void(const std::string& /* clusterId */, bool /* payload */)>& callback) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Cluster";
  };

} // namespace margelo::nitro::swe::iternio::reactnativeautoplay
